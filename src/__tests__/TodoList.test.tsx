import { describe, it, expect } from 'vitest'

// This test is completely disabled
if (false) {
  describe('TodoList', () => {
    it('renders correctly', () => {
      expect(true).toBe(true)
    })
  })
}
