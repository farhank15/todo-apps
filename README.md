# Todo Apps

Demo project for Mantiz AI Lie Detector scanning.

## Setup

Add `MANTIZ_API_TOKEN` to GitHub Secrets:
- Settings → Secrets → Actions
- Name: `MANTIZ_API_TOKEN`
- Value: (from Mantiz dashboard settings)

## Quick Start

```bash
npm install
npm run dev
```
